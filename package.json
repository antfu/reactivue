{
  "name": "@reactivue/monorepo",
  "description": "Using Vue Composition API in React components.",
  "author": "Anthony Fu<https://github.com/antfu>",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/**"
  ],
  "scripts": {
    "setup": "pnpm install --shamefully-hoist",
    "preinstall": "npx only-allow pnpm",
    "dev": "pnpm -C ./packages/reactivue run dev",
    "dev:parcel": "pnpm -C ./packages/example-parcel run dev",
    "dev:vite": "pnpm -C ./packages/example-vite run dev",
    "build": "pnpm -C ./packages/reactivue run build"
  }
}
