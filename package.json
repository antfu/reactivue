{
  "name": "@reactivue/monorepo",
  "description": "Using Vue Composition API in React components.",
  "author": "Anthony Fu<https://github.com/antfu>",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/**"
  ],
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "dev": "pnpm -C ./packages/reactivue run dev",
    "dev:example": "pnpm -C ./packages/example run dev",
    "build": "pnpm -C ./packages/reactivue run build"
  }
}
